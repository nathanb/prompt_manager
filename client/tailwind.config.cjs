/** @type {import('tailwindcss').Config} */

module.exports = {
  mode: 'jit',
  content: [
    './src/**/*.{js,jsx,ts,tsx,mts}'
    // Add paths here if you're using multiple workspaces with React code.
  ]
};
